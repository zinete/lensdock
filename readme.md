<p align="center">
  <h1 align="center">🎥 LensDock</h1>
  <p align="center">A floating camera dock for creators.</p>
  <p align="center">轻量级桌面摄像头浮窗，专为内容创作者打造。</p>
</p>

---

## ✨ 功能特性

- **摄像头浮窗** — 在桌面显示一个透明无边框的摄像头画面，始终置顶
- **自由拖拽** — 拖动浮窗到桌面任意位置
- **形状切换** — 支持圆形、圆角矩形、方形三种裁剪形状，平滑过渡动画
- **大小调整** — 通过设置页面自由调节 80px – 500px
- **镜像翻转** — 一键切换镜像/原始画面
- **边框控制** — 可选显示/隐藏摄像头边框
- **系统托盘** — 左键打开设置页面，右键快捷菜单
- **OBS 兼容** — 可通过 OBS「窗口捕获」直接采集浮窗画面

## 📸 使用场景

- 录制教程视频时在屏幕角落显示个人视角
- 直播时作为摄像头叠加层
- 视频会议时监控自己的画面

## 🚀 快速开始

### 环境要求

- [Node.js](https://nodejs.org/) >= 18
- [Yarn](https://yarnpkg.com/) 或 npm

### 安装依赖

```bash
yarn install
# 或
npm install
```

### 启动应用

```bash
yarn start
# 或
npm start
```

启动后，摄像头浮窗会出现在屏幕右下角，同时在 macOS 菜单栏会出现托盘图标。

## 🎮 操作指南

| 操作 | 说明 |
|---|---|
| **拖拽浮窗** | 直接用鼠标拖动摄像头画面 |
| **左键托盘** | 打开设置页面 |
| **右键托盘** | 快捷菜单（镜像切换、退出） |
| **设置页面** | 调整形状、大小、镜像、边框 |

## 🔌 OBS 集成

1. 打开 OBS Studio
2. 在「来源」中点击 **+** → **窗口捕获**
3. 选择 **LensDock Camera** 窗口
4. 调整图层位置和大小即可

## 📁 项目结构

```
LensDock/
├── assets/                          # 静态资源
│   └── trayIconTemplate.png         # 托盘图标
├── src/
│   ├── main/                        # 主进程
│   │   ├── index.js                 # 应用入口
│   │   ├── settings.js              # 设置状态管理
│   │   ├── windows.js               # 窗口管理
│   │   ├── ipc.js                   # IPC 通信处理
│   │   └── tray.js                  # 系统托盘
│   ├── preload/
│   │   └── preload.js               # 预加载脚本
│   └── renderer/
│       ├── camera/                  # 摄像头浮窗页面
│       │   ├── camera.html
│       │   ├── camera.css
│       │   └── camera.js
│       └── settings/                # 设置页面
│           ├── settings.html
│           ├── settings.css
│           └── settings.js
├── package.json
└── readme.md
```

## 🛠️ 技术栈

- **[Electron](https://www.electronjs.org/)** v40 — 跨平台桌面应用框架
- **WebRTC** (`getUserMedia`) — 摄像头采集
- **CSS `border-radius`** — 形状裁剪与动画过渡
- **IPC** — 主进程与渲染进程的双向通信

## 📄 License

[MIT](LICENSE) © zhenghui
